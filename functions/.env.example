# Copy to .env in this folder (gitignored). Loaded automatically on deploy and at runtime.
# Required only for AI draft callables (draftTranslation, batchDraftTranslations).

TRANSLATION_AI_API_KEY=
TRANSLATION_AI_PROVIDER=openai
TRANSLATION_AI_MODEL=gpt-4o-mini
